import { DataSource } from 'typeorm';
import { createError400, createError404 } from '@shorter/errors';
import { Profile, User } from '@entities';

export const login = async (datasource: DataSource, data: any) => {
  try {
  } catch (err) {
    throw err;
  }
};

export const logout = async (datasource: DataSource, data: any) => {
  try {
  } catch (err) {
    throw err;
  }
};

export const register = async (datasource: DataSource, data: any) => {
  try {
  } catch (err) {
    throw err;
  }
};
